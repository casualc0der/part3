{
  "name": "part3",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.9.25",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "scripts": {
    "build:ui": "rm -rf build && cd ../full-stack-open/part2/phonebook && yarn run build --prod && cp -r build ../../../part3/",
    "deploy": "git push heroku master",
    "deploy:full": "yarn run build:ui && git add . && git commit -m uibuild && yarn run deploy",
    "logs:prod": "heroku logs --tail",
    "start": "node index.js",
    "dev": "nodemon index.js"
  }
}
